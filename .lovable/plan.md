

## Zuper Web Preflight — P-INIT

Update the Index page to show a minimal "Setup in progress" status card, following the design brief exactly. No new routes, components, or backend — just the single page update.

### Changes
1. **Index page** — Replace placeholder with centered status card showing "Zuper Web Preflight" title and "Setup in progress" subtitle, using the design brief's typography, shadow, and layout tokens.
2. **CSS tokens** — Update root CSS variables to match the design brief's color palette (foreground, muted, border).

